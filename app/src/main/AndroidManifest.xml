<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="h1691712.kijiji">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".First" />
        <activity android:name=".Post_advert" />
        <activity android:name=".Categories" />
        <activity android:name=".Location" />
        <activity android:name=".Mykijiji" />
        <activity android:name=".Ad_options" />
        <activity android:name=".Msg" />
        <activity android:name=".BlankMykijiji" />
        <activity android:name=".Favorites"></activity>
    </application>

</manifest>